openmrs-module-vxnaid
==========================

The module which contains metadata, customizations and anything which is required by the Vxnaid Distribution and is not or
 is not supposed to be compatible with other distributions.
